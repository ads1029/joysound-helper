# 工作待办

> 最后更新：2026-07-29
> `GOAL.md` 仍是长期目标、里程碑和关键决策的唯一权威记录；本文只拆分近期执行顺序和验收步骤。优先级变化时必须同步更新两份文档。

## P0：扩大 JOYSOUND X1 曲库覆盖

状态：全曲目发现完成，详情采集阶段性暂停

### 当前机会

- 生产曲库目前有 13,582 首歌曲、20,755 个 X1 版本，按官方“44 万曲以上”的宣传口径估算约占 3.09%，但双方统计口径尚未统一。
- 95 位保留歌手的全曲目发现已经完成：1,108/1,108 个官方列表页成功，错误、待处理和跨页不一致均为 0。
- 完整索引包含 21,195 个唯一歌曲页，其中生产曲库已有 2,908 页、尚未收录 18,287 页；18,287 是详情采集与审计的固定分母。
- 详情阶段当前处理 7,964/18,287 页，成功 7,962、无 X1 2、错误和冲突均为 0；20 首来源复核一致，7,962 首和 9,994 个版本已通过阶段性门禁接入生产，剩余 10,323 页。
- 当前按项目负责人决定阶段性暂停；检查点、歌曲输出、审计报告和生产快照已对齐，恢复起点为 `offset=7964`。

### 下一轮范围

1. [已完成] 使用 `discover:full-artists` 完整读取 95 位保留歌手的官方曲目分页：1,108/1,108 页成功，`discoveryReady=true`。
2. [已完成] 使用独立检查点记录每个歌手的声明总数、计划页数、成功页数、失败页数和候选链接，并自动拒绝历史半页及跨时间排序漂移。
3. [已完成] 按官方歌曲详情 URL 去重并排除接入前的 5,620 首稳定曲库，固定得到 18,287 个详情候选。
4. [已完成] 使用前 20 个详情页验证解析和 X1 命中：20 页成功、20 个 X1 版本、错误和冲突均为 0。
5. [阶段性暂停] 详情采集继续使用单线程、5 秒基础间隔加 0～2 秒随机抖动，每 100 个真实请求冷却 45～60 秒；当前处理 7,964/18,287 页，成功 7,962、无 X1 2、错误 0、冲突 0，剩余 10,323 页，恢复起点为 `offset=7964`。
6. [已完成] 为长周期任务增加阶段性晋级流程：非严格审计后重新复核至少 20 首，使用内容指纹防止过期复核，只允许待处理项存在，并生成精简生产曲库。
7. 完成全部候选后执行严格审计、最终来源复核和完整轮次晋级，并更新 `GOAL.md`、`CRAWLER.md` 和覆盖数量。

### 验收标准

- 95 位目标歌手及其所有声明分页都有明确的成功或不可用状态。
- 入口页处理率为 100%，未解释错误为 0，候选 URL 无重复且来源可追踪。
- 新增详情候选有固定、可审计的分母；成功、无 X1、失效和错误状态之和与分母一致。
- 阶段性生产晋级必须满足请求错误和数据冲突为 0、至少 20 首来源复核一致、复核输入指纹与当前生成歌曲相同。
- 最终待处理、请求错误和数据冲突均为 0，严格审计通过。
- 至少 20 首等距来源复核全部一致，生产数据、测试、构建和目标文档同步完成。

### 后续扩充方向

- 在完成 95 位歌手全曲目后，再从权威歌手榜、年代榜和可审计的官方索引扩展歌手池。
- 调查是否存在覆盖范围更大的官方歌曲 Sitemap 或等价候选索引；未确认分母前不宣称“全曲库覆盖”。
- 候选规模增长后评估前端构建体积和搜索性能，必要时生成紧凑搜索索引。

## P1：建立增量更新

状态：P0 候选索引稳定后执行

- 比较热门 Sitemap `lastmod` 和各候选来源快照，输出新增、变化与失效页面。
- 未变化页面复用检查点，只重新采集新增或来源已变化的详情。
- 每轮记录固定输入快照、状态分母、冲突、复核结果和生产净增数量。

## P2：罗马音搜索

状态：已记录，暂缓

### 需求

- 用户输入日语罗马音时，可匹配对应的平假名、片假名和含汉字歌名。
- 拉丁字母输入不强制判断为英文或罗马音，而是同时执行原文匹配和日语读音匹配，再按完整原文、完整读音、原文片段、读音片段的顺序排序并去重。

### 数据与实现建议

- 优先为歌曲增加经过来源确认的 `readingKana`，再生成罗马音搜索别名。
- 当前只有 20/13,582 首歌曲带 `romaji`，不能只安装转换库就宣称全曲库支持。
- 罗马音与假名转换优先采用成熟库（候选为 WanaKana），不自行实现完整转写算法。
- 若使用形态分析器推测汉字读音，只作为构建期候选，并记录推测来源和人工复核状态；歌曲名、艺名和特殊读法不得未经审核直接进入生产索引。

### 恢复条件

- P0 曲库扩充完成，或已建立稳定、可重复执行的候选及详情采集流程。
- 明确大部分生产歌曲的假名读音来源和覆盖率。
- 为英文标题优先级、长音、促音、拨音、空格、连字符和多种罗马字写法定义回归样例。
